import { Tabs, Tab } from '@rspress/core/theme';
import { Border } from '@components/Border';

В [директории документации](/api/config/config-basic#root) MDX-фрагменты или React-компоненты должны быть исключены из маршрутизации с помощью [route.exclude](/api/config/config-basic#routeexclude). Для удобства файлы, начинающиеся с `_`, исключаются по умолчанию через [route.excludeConvention](/api/config/config-basic#routeexcludeconvention).

Также вы можете размещать компоненты в соседних директориях вне директории docs. Например:

```text
docs
├── _button.mdx
└── index.mdx
components
└── button.tsx
```

<Tabs>
<Tab label="docs/index.mdx">

```mdx
import ButtonFragment from './_button.mdx';
import Button from '../../components/button';

<ButtonFragment />
<Button />
```

</Tab>
<Tab label="docs/_button.mdx">

```mdx file="./_button.mdx"

```

</Tab>
<Tab label="docsite/components/button.tsx">

```tsx
const Button = () => <button>Кнопка из tsx</button>;
export default Button;
```

</Tab>
</Tabs>

Это отображается так:

<Border className='rp-toc-exclude'>

import Button from './_button.mdx';

<Button />
<button>Кнопка из tsx</button>

</Border>